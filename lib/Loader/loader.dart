import 'package:flutter/material.dart';
import 'package:app/Loader/components/body.dart';

class Loader extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
